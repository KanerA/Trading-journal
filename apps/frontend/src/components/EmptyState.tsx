import { Box } from "@mui/material";

const EmptyState = () => (
    <Box sx={{
        margin: "3rem",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
    }}>
        COMING SOON
    </Box>
);

export default EmptyState